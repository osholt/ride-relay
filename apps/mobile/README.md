# Ride Relay mobile app

The shared Flutter client and native iOS/Android shells. Run all commands in
this directory unless using the repository `Makefile`.

The current native method channel reports platform capability state. Google
Nearby Connections is deliberately not marked ready until the Phase 0
cross-platform/background field-test gate passes.
